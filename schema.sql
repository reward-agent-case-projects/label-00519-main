-- Create database if not exists
CREATE DATABASE IF NOT EXISTS hangfire_db;
USE hangfire_db;

-- Hangfire tables will be automatically created by the application on startup.
-- No explicit schema is needed for Hangfire itself.
